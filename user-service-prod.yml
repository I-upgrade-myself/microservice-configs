server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: dev_user
    password: dev_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
